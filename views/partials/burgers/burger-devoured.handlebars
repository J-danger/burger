<div class="mt-4">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>
	<!--Delete button-->
<form class="devour-form" action="burgers/update">
	<button class="delete-burger btn" id="deleteButton" data-id="{{this.id}}"><i class="far fa-trash-alt"></i> Throw away</button>
</div>

</form>