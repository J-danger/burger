<div class="mt-4">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>
	<!--Throw away burger button-->
<form class="devour-form" action="burgers/update" method="DELETE">
	<button class="delete-burger btn" data-id="{{this.id}}"><i class="far fa-trash-alt"></i> Throw away</button>
</div>

</form>