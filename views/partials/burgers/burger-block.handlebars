<form class="devour-form" action="burgers/update/{{id}}" method="PUT">
<div class="mt-4">
	<h4>{{burger_name}}</h4>
	<!--Devour me button-->
	<button class="change-devour btn" data-id="{{id}}" data-newdevour="{{devour}}">
	 Devour me
	</button>
	
</div></form>